/* eslint-disable getter-return */
/* eslint-disable no-empty-function */
/* eslint-disable no-unused-vars */
/* eslint-disable class-methods-use-this */
export default class SchedulerGroup {
  /**
   * Allows to schedule on the current task.
   */
  get immediate() {
  }

  /**
   * Allows to schedule on the micro tasks.
   */
  get micro() {
  }

  /**
   * Allows to schedule on the macro tasks.
   */
  get macro() {
  }
}
